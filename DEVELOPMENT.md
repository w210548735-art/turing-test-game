# 开发状态

本文件仅记录可公开的功能状态和验证结果。真实服务器地址、账户邮箱、备份
哈希、发布路径、凭据轮换和事故处理记录必须写入被 Git 忽略的
`DEVELOPMENT.private.md`。

## 当前能力

- 已验证邮箱账户注册、验证邮件重发、登录、密码恢复、会话轮换与注销；
- 真人优先匹配、统一搜索与入场门、AI 配额控制；
- 匿名聊天安全处理、举报、反馈与回声档案；
- 玩家身份资料、战绩页和 ROOT 运营概览；
- PostgreSQL、Redis、HTTPS/WSS 与容器化部署。

## 公开验证基线

- TypeScript 类型检查通过；
- 协议测试 34/34；
- 服务端测试 122/122；
- 前端测试 68/68；
- 合计 224/224；
- 生产构建通过。

## 更新规则

公开记录可以包含功能、测试数量、迁移编号和通用兼容性结论，不得包含：

- 真实邮箱、电话号码、服务器 IP、实例 ID 或内网拓扑；
- Token、密码、授权码、Cookie、密钥摘要或真实 Secret 路径；
- 数据库备份文件名、校验哈希、精确发布时间和生产 release 路径；
- 可用于绕过安全控制的内部阈值、处置流程或事故细节。

## 2026-07-26：验证邮件重发与 SMTP 告警

- 注册入口新增“重新发送验证邮件”，仅要求填写邮箱；
- 公共响应不区分邮箱不存在、已激活或仍待验证，避免账号枚举；
- 待验证账户重发时原子撤销同用途旧 Token，新邮件链接成为唯一有效链接；
- 重发沿用邮件发送复合限流，覆盖邮箱、来源地址、设备与全局维度；
- SMTP 投递失败保持公共接口 `202`，同时写入不含邮箱、Token 或错误正文的
  结构化告警；服务内监控快照记录 `ok/degraded`、连续失败数、累计失败数和
  最近失败时间，但公共健康接口不暴露这些计数，避免账号枚举旁路；
- 告警回调异常会被隔离，不会改变注册、重发或密码恢复的公共响应；
- 已完成浏览器交互验收、失败投递自动化测试，以及一次经授权的 QQ SMTP
  真实连接与单封重发测试。测试邮件使用临时内存账户，链接不对应持久化环境。

## 2026-07-26：作者联系入口补充

- “关注作者”弹窗由三个平台入口扩展为四项 2×2 布局；
- 新增商务合作微信联系项，并提示添加时备注来意；
- 商务联系项为静态信息展示，不伪装成外部跳转链接。
